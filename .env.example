# Database - SQLite for development (change to PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# NextAuth - Generate a secure secret for production: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# App config
NEXT_PUBLIC_APP_NAME="CleanPro Manager"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# For PostgreSQL production:
# DATABASE_URL="postgresql://user:password@host:5432/cleanpro_db?schema=public"
